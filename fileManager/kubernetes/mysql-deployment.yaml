---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: filemanager-db
  namespace: gasalho
  labels:
    app: testdb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testdb
  template:
    metadata:
      labels:
        app: testdb
    spec:
      containers:
      - name: filemanager-db
        image: mysql
        imagePullPolicy: IfNotPresent
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: filemanagerapi-secrets
              key: DB_PASSWORD
        ports:
        - containerPort: 3306
          name: testdb-contain
        volumeMounts:
          - name: mysql-persistent-storage
            mountPath: /var/lib/mysql
      volumes:
        - name: mysql-persistent-storage
          persistentVolumeClaim:
            claimName: mysql-pv2-claim
---
apiVersion: v1
kind: Service
metadata:
  name: filemanager-db
  namespace: gasalho
  labels:
    app: testdb
spec:
  ports:
  - port: 3306
    protocol: TCP
    name: filemanager-db
  selector:
    app: testdb